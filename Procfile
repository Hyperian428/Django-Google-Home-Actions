web: daphne APbackend.asgi:application --port $PORT --bind 0.0.0.0
#web: gunicorn APbackend.wsgi
# this is based on the channel 2.0 example
#web: daphne APbackend.asgi:application --port $PORT --bind 0.0.0.0
# "gettingstarted" is the project file name. wsgi is the .py file
# it uses gunicorn command because it's how my python code talks to the web server by using the wsgi file.
